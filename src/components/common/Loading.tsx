import { FC } from "react";

const Loading: FC = () => {
	return (
		<div className="flex h-screen w-screen">
			<div className="text-grey m-auto text-xl">loading...</div>
		</div>
	);
};

export default Loading;
